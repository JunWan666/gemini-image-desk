# Agent Rules

这些规则用于后续协作者或编码代理继续实现 Gemini Image Desk。

## Product Direction

- 这是一个 Gemini 专用图像生成工作台，不是通用聊天应用。
- 产品必须支持 i18n，默认语言是中文，英文作为第二语言。
- 第一屏必须是可用的工作台，不要做 landing page。
- 优先完成核心闭环：prompt、参考图、生成、编辑、历史、下载。
- 不复制 `img.1415.xin` 的 UI，只保留它高效、少废话的工作流精神。
- 不引入账号系统、云图库、支付、分享社区等第一版不需要的功能。

## Implementation Rules

- 默认使用 Next.js + React + TypeScript。
- UI 文案必须通过 i18n 字典读取，不要在组件中硬编码中文或英文。
- 第一版至少提供 `zh-CN` 和 `en-US` 两套字典，默认 `zh-CN`。
- API 调用必须通过 `lib/providers/` 下的 provider adapter，React 组件不得直接拼 Gemini 请求。
- API Key 默认只保存在浏览器本地，不写入仓库、不输出到日志。
- 网络错误、模型错误、鉴权错误必须统一转换成用户可读的错误对象。
- 图片历史第一版使用浏览器本地存储，后续再考虑数据库。
- 文件上传和图片预览要限制大小，并在 UI 中给出清晰错误。
- 不为单个页面过早抽象复杂状态管理；当状态跨三块以上 UI 共享时再提取 store。

## UI Rules

- 如果用户提供 Figma MCP 和目标文件，先在 Figma 中确认主工作台设计，再实现前端。
- 使用三栏工具布局：左配置、中创作、右参数/历史。
- 不使用营销式 hero、夸张渐变背景或装饰性大卡片。
- 不把卡片套进卡片。页面分区使用面板、分隔线和工具栏。
- 工具按钮优先使用图标，并提供 tooltip。
- 文本、按钮、缩略图、工具栏必须有稳定尺寸，避免生成状态造成布局跳动。
- 所有长 prompt、错误信息、模型名称都必须可换行或截断，不能撑破容器。
- 调色应克制，允许少量 Google-ish 蓝、绿、黄、红作为状态点缀，但不要做单一蓝紫主题。

## Security Rules

- 不提交真实 API Key、截图中的 Key、示例 Key。
- 不在 console、server log、error message 中输出完整 API Key。
- 本地保存 API Key 时要让用户能一键清除。
- 若未来支持服务端代理，请明确区分用户输入 Key 和服务器环境变量 Key。

## Code Review Checklist

- 这个改动是否推进了第一版核心闭环？
- 是否引入了和 Gemini API 耦合过深的 UI 代码？
- 是否处理了加载、失败、空状态？
- 是否保护了 API Key？
- 是否在窄屏下仍然可用？
- 是否有最小测试覆盖高风险逻辑？
